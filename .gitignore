node_modules/
dist/
*.tsbuildinfo
# generated fixture repos (each carries its own inner .git; built by fixtures/*.build.mjs)
fixtures/vulnerable-app/
# local run outputs
scan-result.json
rampscan-out/
rampscan-ledger/
rampscan-projection.db
exports/
# incremental scan cache (content-addressed collector results; rebuildable)
rampscan-cache/
# local signing keypair — the private key must never be committed
rampscan-keys/
# vendored PocketBase binary (fetched by scripts/fetch-pocketbase.mjs against the pin) + its data
console/pocketbase/bin/
console/pocketbase/data/
# Next.js build output
console/web/.next/
.gstack/
