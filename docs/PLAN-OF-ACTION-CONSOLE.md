# rampscan — plan of action: console personas (operator + auditor)

**Status:** working checklist — the executable layer for the post-H console track. `docs/PLAN-OF-ACTION.md` owns the prototype's history (Phases A–H, complete); this document owns the console persona work: task order, exit tests, current status. All items here have landed — I3e and I3f both on 2026-08-15, closing Phase I3; their task breakdown lives in `docs/IMPLEMENTATION-PLAN-REMAINING.md` §3 and §7. Both lenses gained K1's plain-language layer on 2026-08-15 (`docs/IMPLEMENTATION-PLAN-REMAINING.md` §8): authored operator English on board rows, evidence pages and the queue, glossary-on-hover over the jargon, and an unevidenced row that says why it is empty in the run record's own words. L1 (2026-08-15, `docs/IMPLEMENTATION-PLAN-ONTOLOGY-ONDEVICE-LLM.md`) added **no new console surface** by design — the architecture-contract rows ride the board, the queue and the evidence page like any recipe. Its two console-side changes are both existing surfaces rendering an existing shape: the I3f/J5 basis panel now prints the declared rules a contract verdict was checked against (the declaration itself, never a paraphrase — the reader is deciding whether the verdict matches what the repository said about itself), and the glossary gained the contract vocabulary. L2 (2026-08-16, same document) added **no new console surface either**, and needed only one new J4 artifact family: `repo-model.json` is a subject of the **run record**, and the evidence page already renders every subject of every statement through the viewer, so the model reads on the run's own page like any artifact — nodes as rows, each node's outgoing links in its own column, and no coverage number, because the board owns the counts. L2 did fix a real console-facing bug in J4's resolver: a subject was classified as a scoping justification whenever its statement was not an evidence bundle, which had never shown because every artifact until now was also an evidence bundle's subject — `repo-model.json` is the first one a run record alone attests, and it refused to serve. On any architecture dispute, `docs/SPEC.md` wins.
**Date started:** 2026-08-14
**Decision of record (2026-08-14):** one console, two lenses — not two products. The operator lens answers "what do I act on today"; the auditor lens answers "prove it, trace it, let me take it with me." Both are projections over the same ledger, role-gated in the existing console (`users.role`), never forked.

---

## 0. Ground rules, active from the first commit

1. **One console, two lenses.** Auditor and operator views are role-gated pages over the same projections. No second app, no second data path — two renderings that drift apart would violate the honesty principle at the UI layer.
2. **Registers stay the record.** Workflow state (queue triage, assignments, acknowledgments) lives in console-writable PocketBase collections — the same posture as proposals — never in the projection collections. Nothing an operator clicks may mute, hide, or reorder a register row's verdict.
3. **The auditor lens is strictly read-only and export-heavy.** Every rendered claim carries its bundle digest and an offline verification path. The console asks to be distrusted: "verify this yourself" is a feature, not an apology.
4. **Computed, never typed** (inherited). Coverage percentages, gap durations, queue rankings — every number on a page comes from a fold or a query, not a keyboard.
5. **Console tests ride with the first UI slice.** The console currently has zero automated tests; the first Playwright smoke is part of I2's exit, not a cleanup task. Every later phase extends it.
6. **New views are projections.** No new ledger statement kinds in this track — the ledger already holds everything these pages need. (If a phase discovers otherwise, that's a spec conversation first.)

---

## Phase I1 — substrate: projections both lenses need

- [x] I1a. **Control register projection** in `packages/projector` (`fold.ts`): control → mapped recipes → rolled-up verdict (`violated` beats `unevidenced` beats `evidenced`; scoping honored per existing precedence) + coverage counts ("2 of 3 mapped recipes evidenced"). Same for a KSI register. SQLite + PocketBase writers, same drop-and-refill posture.
- [x] I1b. **As-of fold**: `fold(ledger, { asOf })` — deterministic point-in-time projection from the append-only record. CLI surface first: `rampscan board --as-of <iso>` (cheapest honest demo of the capability).
- [x] I1c. **mvx twin test** — closes the drift gap found 2026-08-14: one test imports both `packages/scheduler/src/mvx.ts` and `console/web/lib/mvx.ts` and asserts the thresholds agree (fresh < 0.75 ≤ expiring < 1.0 ≤ expired). The comment "thresholds agree by test" becomes true.
- [x] I1d. **Cadence-adherence history**: per-recipe freshness gaps derived from bundle chains × MVX windows — every interval where live evidence sat past 1.0, with start/end/duration. Exposed in the projection; the clock view and the auditor's gap timeline both read it.

**Exit test:** e2e — two scans with a touched file between them; folding as-of a timestamp between the scans reproduces byte-for-byte the projection the first scan produced. Control-register counts match an independent recount from rows (report-test posture). mvx twin test green. `pnpm test` green throughout.

---

## Phase I2 — operator lens: from status display to work queue

- [x] I2a. **Action queue page**: one ranked list merging cache-divergence alerts, evidence entering the expiring band before its next cadence refresh, new violations (born/verdict-flipped drift events), and actionable unevidenced rows (collector skipped for a fixable reason, e.g. tool missing → doctor hint). Ranking: divergence > expiring-before-next-cadence > new violation > actionable unevidenced. Pure re-projection + daemon events; no new writes.
- [x] I2b. **Daemon status strip** on the board header: last scan (commit + time), daemon alive/stale (no tick past the check interval → say so loudly), next expected cadence action, divergence-report presence. Source: `daemon-events.jsonl` (`serve` tails it into a console-readable collection). An operator who cannot see the daemon died owns a silently rotting board. *(Landed with one deliberate deviation: alive/stale needed a heartbeat, and quiet ticks wrote nothing — the heartbeat lives in a `daemon-status.json` SNAPSHOT the daemon overwrites each tick, mirrored into `daemon_status`, because one tick per check interval appended to the events file would drown the mirror's 500-line tail and push a standing divergence off the queue.)*
- [x] I2c. **Fix pointers on violation rows**: failing file/line where the artifact carries it (semgrep, reachability `call_path`), reproduce command, killing/introducing commit — on the row or one click deep, never a context switch away. *(Landed as structured `offenders` on the assertion result + a per-recipe `reproduce` in the predicate, both deliberately excluded from evidence identity — existing bundles survive and gain pointers only when real drift re-keys them.)*
- [x] I2d. **"Since baseline" drift toggle**: diff the board against a chosen prior scan (default: previous), not only the by-day grouping. *(Landed as one hand computing for both surfaces: `computeBoardDiff` in @rampscan/cli — two as-of folds (I1b) plus a pure state-level register diff in the projector — called by `rampscan board --since previous|<iso>` AND the console's read-only `/api/board/diff` route, so terminal and browser can never disagree. "A scan" is a distinct evidence predicate timestamp — the join's single clock makes that exact.)*
- [x] I2e. **First Playwright smoke** (ground rule 5): `rampscan serve` → login → board renders rows from a real fixture scan → evidence detail shows assertions + call path → action queue renders ranked. Runs in CI against the fixture. *(Landed with the prepare steps INSIDE the webServer command — Playwright starts the webServer before global setup runs, so a setup-file scan would race the serve it feeds. The smoke serves a scratch `--pb-data` so it can never touch the dev console's users or pending proposals.)*

**Exit test:** with the daemon stopped, the console visibly says so. The daemon e2e's poisoned-cache divergence scenario surfaces at the top of the queue. Playwright smoke green in CI.

---

## Phase I3 — auditor lens: verify, trace, take away

- [x] I3a. **Control register page**: the auditor's landing view — control ID → mapped recipes → verdicts → evidence bundles, every hop clickable in both directions (bundle → recipe → KSI → control and back). *(Landed as `/controls` rendering BOTH rollup registers (Controls/KSIs tabs) straight off the I1a projections — no new fold, no new collections; expansion joins the live `registers` collection, so the sub-rows are the board's rows, never a copy. The back direction is a deep link: `?reg=…&id=…` arrives expanded + highlighted, linked from the evidence page's ksi/control ids and the board's cells.)*
- [x] I3b. **Verify-this-yourself block** on every evidence page: download raw DSSE bundle + public key, copy-paste offline `rampscan verify <digest>` invocation. Page header surfaces what the bundle already carries: dataset pin, tool versions, extractor version, scanned commit. *(Landed as two read-only auth-gated routes streaming the ledger's EXACT envelope bytes + the SPKI PEM — never a re-serialization of the projection's copy; the invocation quotes `verifyCommand` stamped by serve with its REAL --ledger/--keys dirs. The predicate carries no separate extractor-version field — tool_versions is what the bundle carries, surfaced honestly. Smoke proves the downloads verify with node:crypto ALONE, no rampscan code.)*
- [x] I3c. **Scoping register**: every scoping decision — approved *and rejected* proposals — with full justification text, proposer + approver identities, timestamps, signature verification status, and the recipe/controls removed from scope. N/A is where auditors sample hardest; the honest record of a rejected scope-out is a strength. *(Landed as `/scoping` over `computeScopingRegister` in @rampscan/cli — approved rows read from the LEDGER's signed scoping events with each DSSE envelope re-verified on every load (the same primitives `rampscan verify` uses; the page never quotes a verification it didn't just run), rejected/pending rows from the proposals collection — the only place a rejection exists, said out loud. Cross-checked, never merged blindly: an approved claim the ledger cannot back renders a loud `missing` row.)*
- [x] I3d. **As-of selector** on board + control register (UI over I1b) and the **cadence-gap timeline** in the clock view (UI over I1d): "did evidence lapse during the assessment period, and for how long?" *(Landed as one hand for both surfaces: `computeBoardAsOf` in @rampscan/cli — one as-of fold (I1b) plus the ledger's scan instants — called by `rampscan board --as-of` AND the console's read-only GET `/api/board/asof`; the board and control register grow an "as of" toggle (datetime input + jump-to-scan quick picks) that renders the historical fold through the SAME row components via mechanical field renaming, propose-N/A suppressed because a historical fold offers no actions to take today. The gap timeline reads the projected `gaps` collection straight — per-cell lapse tracks on one shared time domain ending at the fold's projected_at (never this browser's clock), ongoing lapses striped and said out loud, every segment linking to the bundle whose window closed.)*
- [x] I3e. **Export**: CSV of any register view; per-control evidence package (bundles + artifacts + public key + verify instructions, zipped); print-friendly evidence page (CSS, not a PDF service). *(Landed as `computeEvidencePackage` in @rampscan/cli — one hand, the console's read-only `/api/export/control` route calls it — assembling a hand-rolled ustar **tar**, not a zip: no dependency for a bounded 512-byte-layout job, same house habit as DSSE-without-cosign. Two honesty rules do the load-bearing work: artifacts are matched by DIGEST, never by name (the scan output dir is overwritten by later runs, so a same-named file is refused with the reason rather than shipped), and every mapped recipe appears in the manifest INCLUDING the unevidenced ones. Anchors are named, not bundled — they are the client's own source, and each carries the `git show` line that reproduces it. CSV is console-side and pure, exported from the rows already rendered (filters and as-of instant included), with the fold instant on every row rather than a preamble block: a preamble breaks strict parsers and makes "row count" ambiguous, and a lifted row should still date itself.)*
- [x] I3f. **Not-affected claims show their work**: entry-point set and its provenance (config override vs package.json inference), the over-approximation statement rendered inline ("not-affected only when even the loose walk cannot reach the package; unknowns count against us"), exact-vs-inferred edge marking on displayed paths. *(Landed 2026-08-15, folded into J5 as planned — same rendering problem, same pages, same reader. The basis is SIGNED WITH THE CLAIM rather than looked up beside it: `graph.db` is a binary artifact the browser cannot parse and is not even a subject of the SAST bundle, so a console that fetched the entry-point set from anywhere else would be describing a different walk than the one that produced the verdict above it. `ClaimBasis` on the evidence predicate carries the entry-point set, its provenance rendered as a sentence naming what the reader can DO about it, any declared entry that resolved to no walked file (a dropped root silently widens every not-affected claim), the route roots, the graph's own shape including how many edges are only name-inferred, and a `degraded` reason when the gate ran without a graph or without roots. Exact-vs-inferred marking is PER HOP, not per path: `shortestPath` already knew each edge's resolution and threw it away in an OR, and the marks ride the I2c offender pointer — accepted only when their length matches the path they claim to describe, because a short array shifts every mark onto the wrong edge and a wrong mark is worse than none. The basis is keyed into evidence identity, which closes a real hole: this collector anchors the flagged files and rampscan.config.json but not package.json, so a package.json edit that moved entry-point inference used to leave the stale claim alive.)*

**Exit test:** an assessor-shaped Playwright walkthrough — pick a control on the control register → traverse to an evidence bundle → download the evidence package → `rampscan verify` passes offline on the downloaded bundle. Scoping register shows one approved and one rejected proposal with verifiable signatures. Exported CSV row count equals the on-screen register. Print view of an evidence page contains digest, commit, dataset pin, tool versions.

---

## Deferred by name (do not build in this track)

Auditor SSO / external assessor accounts · console access audit log · acknowledge-as-ledger-event (workflow state stays console-local until a real need names it) · PDF rendering service (print CSS only) · multi-repo joins · React Flow canvas · webhooks · CI wiring for the Phase H collector families (still the next *engine* track, deliberately not this one).

---

## Session log

Update as work lands — newest first.

| Date | Phase | What landed | Notes / deviations |
|---|---|---|---|
| 2026-08-15 | I3f | **Not-affected claims show their work** — landed folded into J5, as the plan said it should be: same rendering problem, same pages, same reader. **The basis is SIGNED WITH THE CLAIM, not looked up beside it**, and the reason is structural: `graph.db` is a binary artifact no browser can parse (J4 refused to table it for exactly that reason) and it is not even a subject of the SAST bundle — those subjects are anchor files — so a console that fetched the entry-point set from anywhere else would be describing a different walk than the one that produced the verdict above it. `ClaimBasis` on the evidence predicate carries: the entry-point set; its provenance (`config` \| `package.json` \| `fallback` \| `none` \| `unavailable`, each rendered as a sentence naming what the reader can DO about it, not as a bare enum); any declared entry that resolved to **no walked file**, called out loudly because a dropped root silently shrinks the reachable set and therefore silently WIDENS every not-affected claim; the declared routes that also seeded the walk; the graph's own shape (nodes, edges, and how many edges were matched by name rather than resolved); and a `degraded` reason when the gate ran with no graph or no roots. The over-approximation statement is the collector's own sentence — "unknowns count against us" — signed into every bundle and rendered inline where the claim is read, and the two gates' `approximation: "over"` sits opposite `route-auth-coverage`'s `"under"`, because "this route reaches auth" is a POSITIVE claim that may only rest on a real call chain and the two walks must not read alike. **Exact-vs-inferred marking is PER HOP, not per path**: `shortestPath` already knew each edge's resolution and threw it away in a single OR, so it now returns `resolutions[]`, and the marks ride the I2c offender pointer (already outside evidence identity) — accepted only when their length matches the path they claim to describe, since a short array shifts every mark onto the wrong edge and a wrong mark on a call path is worse than no mark; pre-I3f evidence renders `unmarked`, never assumed exact. The basis is **keyed into evidence identity**, which closes a concrete hole: `sast-reachability` anchors the flagged files and `rampscan.config.json` but NOT `package.json`, so with the basis outside identity, an edit that moved entry-point inference left every not-affected bundle alive claiming unreachability from a set that no longer existed. | Smoke test 13 walks it on the real fixture: the entry-point set and "inferred from package.json" on screen, the over-approximation statement inline, the graph's counted shape, and the reachable eval's call path with every hop marked and zero unmarked hops. Ten vitest cases drive the shipping gate over a real signed ledger — including an app with NO detectable entry point, where the basis records `degraded` and the hit still COUNTS, because a walk with no root waives nothing. **Phase I3 is complete.** |
| 2026-08-15 | I3e | Export — the auditor takes the record away, and Phase I3's written exit test can finally run. **The per-control evidence package**: `computeEvidencePackage` (@rampscan/cli) is the one hand — one fold of the ledger, then byte-exact reads of the object store — called by the console's new read-only GET `/api/export/control` in the diff route's exact posture (auth-refresh gate, `runtime: nodejs`, structurally unable to write). It ships a hand-rolled ustar **tar, not a zip**: ustar is a fixed 512-byte layout, the job is bounded and testable, and this house has declined a dependency for exactly this shape of work before (DSSE without cosign, setTimeout without node-cron); uncompressed on purpose too — `tar xf` needs no flags and the member names are legible in the raw bytes. Contents: `bundles/<digest>.envelope.json` byte-for-byte from the object store (never a re-serialization — I3b's rule, for I3b's reason), the artifacts those statements attest, `rampscan.pub`, `MANIFEST.json`, and a generated `VERIFY.md` carrying both the crypto-only recipe and the serve's OWN verify invocation (built from the same env `serve` stamps `verifyCommand` from, so it quotes real dirs and never a guess). **Two honesty rules do the load-bearing work**: (1) artifacts are matched by **digest, never by name** — the scan output dir is overwritten by later runs, so a same-named file from a later run is refused with the reason stated (`not retained — the file of this name on disk is from a later run`) rather than shipped under an attested digest; (2) **every mapped recipe appears in the manifest, unevidenced ones included** — a package carrying only the good news is the screenshot folder this product exists to replace. Subjects are classified rather than dumped: an `anchor` (a subject named by `anchor_paths`) is the client's OWN source and is named, not bundled, each carrying the `git show <commit>:<path>` line that reproduces it — a package that quietly bundled source would be exporting the codebase instead of the evidence about it; a scoping event's `justification.txt` subject is already inside its signed predicate, so a second copy would be a second thing to disagree. **CSV**: `console/web/lib/export.ts`, pure, exported from the rows the page is ALREADY rendering (filters and as-of instant included — a CSV built from a second query would be a different answer that merely looks like the same one). The fold instant rides **every row** rather than a preamble block: a preamble breaks strict parsers and makes "row count" ambiguous, and a single row lifted out of the file should still date itself. Buttons on the board, both rollup registers, and the scoping register; the package button is suppressed under a historical fold (it is assembled from the live ledger — offering it there would hand back a package that doesn't match the rows on screen; I3d's propose-N/A precedent). **Print**: `@media print` in globals.css — nav/buttons/filters/strips gone, digest + commit + dataset pin + tool versions + assertions forced visible, `overflow`/`white-space` unwrapped because truncation is a screen affordance and on paper it is a withheld fact. CSS only; the PDF service stays deferred by name. I3b's local `DownloadButton` extracted to `components/` (one download posture for the console: authorized fetch → blob, since a bare `<a href>` carries no PB token) and taught to take its filename from Content-Disposition, which the package needs. Tests: vitest **324/324** (23 new — a reference untar that re-checks every ustar checksum, digest-mismatch refusal, anchor naming, no-artifacts-dir honesty, a recount of the manifest's own counts, plus the CSV twin loaded by path in the mvx-twin posture). Smoke grew **test 9**, the Phase I3 exit test end to end: /controls → ra-5 → download the package → verify the flagship's envelope with **node:crypto ONLY against the package's own bytes** (payload hashes to its address, ECDSA P-256 over the DSSE PAE against the tar's own `rampscan.pub`) → every shipped artifact hashes to the digest in its path → run the manifest's OWN verifyCommand verbatim ("signature ok" + "payload  ok") → CSV row counts equal the counts the page renders, on the rollup register AND on the board AND under the Violated filter (an export that ignored filters would still "match" an unfiltered screen) → print media emulated: digest/commit/dataset pin/tool versions survive, nav hidden. Smoke **9/9 cold (6.9m)**, root + console typecheck clean. | Found en route: the first `tsc --build` after adding the CSV twin test emitted `export.js`/`export.d.ts`/`types.js`/`types.d.ts` **into `console/web/lib/`** — the twin must be loaded by a runtime path (the `beforeAll` dynamic-import shape the status/queue twins already use), never a static `typeof import(...)`, or the console's lib dir grows compiled duplicates Next could resolve instead of the sources. Turds deleted; the fixed build does not re-emit. Also: `serve` now passes `RAMPSCAN_OUT_DIR` (already resolved absolute, like every other dir it exports) so the package can find artifacts — without it the export still works and says why they are absent. next-env.d.ts restored from the smoke's `.next-smoke` stamp again, as always. Next action: J1 (`scan-run` statement kind + the exec journal at the tools.ts seam — see `docs/IMPLEMENTATION-PLAN-REMAINING.md` §4). |
| 2026-08-14 | I3d | The as-of selector + the cadence-gap timeline — the auditor can now stand at any past instant and read the record, and see every interval where evidence lapsed. One hand computes the past for both surfaces: `computeBoardAsOf` (@rampscan/cli) = ledger list + recipes + ONE as-of fold (I1b) + `scanInstants`, called by `rampscan board --as-of` (the CLI branch now goes through it, so terminal and browser literally share the code path) AND the console's new read-only GET `/api/board/asof?at=` in the diff route's exact posture (auth-refresh gate, `runtime: nodejs`, structurally unable to write). The board and the control register both grew an "as of" toggle — datetime input + "jump to a scan" quick-picks enumerated from the ledger's scan instants — and render the historical fold through the SAME row components the live board uses, via mechanical camelCase→record-shape renaming in `lib/asof.ts` (shared hook `useAsOfBoard`, refetched when the live projection moves; renaming, never recomputation). Honesty in the UI: the as-of strip labels the view loudly ("historical view, read-only", "(a scan instant)" when it is one), propose-N/A is suppressed because a historical fold offers no actions to take today, an instant before all evidence renders "no ledger statement at or before it" as an answer rather than an error, and turning as-of on parks the since-baseline diff (the two lenses on the past stay distinct). Deliberate: `computeBoardAsOf` passes no MVX window — an as-of fold's "ongoing" gap would end at the fold clock, not the chosen instant, and quoting that as history would be an invented claim; the gap timeline instead reads the LIVE projected `gaps` collection (I1d) straight: the clock view's new "Cadence lapses" section renders per-cell lapse tracks on one shared time domain ending at the fold's projected_at (never this browser's clock), ongoing lapses striped + counted out loud ("N ONGOING — evidence is lapsed right now"), every segment and sub-line linking to the bundle whose window closed unrefreshed, and the empty state saying "no cadence lapse on record" instead of rendering nothing. Vitest: 3 new tests in the ledger e2e's real two-scan world — as-of between the scans reproduces scan 1's registers AND rollups byte-for-byte (canonicalJson), as-of AT a scan instant includes it and says it is one, as-of before any scan folds an honestly empty world. Smoke grew test 8: as-of on → refold at now agrees with the live board (flagship still violated) → rewind to 2000 → honest empty → quick-pick the scan instant → the scan's board returns with "(a scan instant)" → same walk on /controls (ra-5 violated, then honestly empty) → /clock shows the lapse section with the honest no-lapse record. Tests: smoke 8/8 cold (fresh .smoke + .next-smoke, 5.1m) and warm (5.7m); vitest 301/301 (3 new); root + console typecheck clean. | Found en route: yesterday's I3c scoping-register suite carried a time bomb — `scopedWorld()` stamped its scoping event with the WALL CLOCK while the rejected-proposal fixture pinned `updated: …T11:00Z` and asserted it sorts newer; the suite started failing the moment real UTC passed 11:00. Fixed by pinning `recordScoping`'s `now` in the test world — the fold's own rule (no wall clocks in computed claims) applied to its tests. Also: the smoke's next-env.d.ts `.next-smoke` stamp survived BOTH completed runs (not just a killed one, as f754026 assumed) — restored to `.next` in this commit; the exit-handler restore evidently does not run under Playwright's webServer teardown. Next action: I3e (export — CSV of any register view, zipped per-control evidence package, print-friendly evidence page). |
| 2026-08-14 | I3c | The scoping register — every scoping decision on one auditable page, approved AND rejected. One hand computes: `computeScopingRegister` (@rampscan/cli) reads approved rows from the LEDGER's signed scoping events, re-verifying each DSSE envelope at compute time with the same primitives `rampscan verify` uses (signature over the PAE, signed payload byte-equal to the stored statement, payload hashing to its address — the page never quotes a "verified" it didn't just check); rejected and pending rows come from the console's `proposals` collection, because a rejection never touches the ledger — it exists nowhere else, and the page says so instead of dressing it up. Cross-checked, never merged blindly: an approved claim whose digest the ledger cannot produce renders a loud `missing` row with the problem named; a ledger scoping with no matching proposal row is flagged as recorded outside the two-key flow; a proposal whose recipe left the catalog says so rather than guessing its scope. Signature statuses all test-pinned (vitest, real ledger + real keys): `verified` / `failed` (wrong serving key) / `unsigned` (appended without envelope) / `missing` (workflow claim the ledger can't back). Console: read-only GET `/api/scoping/register` in the diff route's exact posture (auth-refresh gate, `runtime: nodejs`, structurally unable to write) feeds `/scoping` — decision pills (scoped out / rejected / pending), full justification text, both identities, timestamp, signature pill, digest → evidence-page hop (I3b's downloads work on scoping bundles too), and the removed-from-scope ksi/control ids linking into I3a's register; refetch keyed on proposals changes + projected_at; `problems` rendered as error lines, never smoothed over. Smoke grew test 7 — the first end-to-end coverage of the two-key flow: file two proposals, the approver approves one (a REAL signed scoping event appended to the scratch ledger by the decide route) and rejects the other, `/scoping` shows the approved row signature-verified with the signed predicate's identities and the rejected row kept honestly ("kept in scope"), then the digest hop lands on the scoping bundle's evidence page showing the signed justification. Tests: smoke 7/7 cold (fresh .smoke + .next-smoke, 5.0m) and warm — the warm run re-ran the mutating test against a world already carrying the first run's decisions, so rerun-idempotency is proven, not assumed; vitest 298/298 (7 new), root + console typecheck clean. | Found en route: a fully-tooled local scan of the fixture leaves ZERO unevidenced rows (13 violated, 2 evidenced) — the board's propose-N/A affordance only renders on unevidenced rows, so the smoke files its proposals via the `proposals` collection directly (the form's exact write, same token, same fields) and keeps its UI coverage on what I3c adds: the second key turn and the register. Also committed separately: next-env.d.ts restored to the `.next` reference — the I3a commit had frozen the file mid-smoke with `.next-smoke` stamped. Next action: I3d (as-of selector on board + control register, cadence-gap timeline in the clock view — both UI over I1b/I1d). |
| 2026-08-14 | I3b | The verify-this-yourself block — the console now asks to be distrusted with artifacts, not just words. Two new read-only routes in the diff route's exact posture (auth-refresh gate, `runtime: nodejs`, structurally unable to write): `GET /api/verify/bundle?digest=` streams the ledger object store's `<digest>.envelope.json` **byte-for-byte** (never a re-serialization of the projection's copy — the envelope's base64 payload IS the canonical statement, sha256 of the decoded bytes reproduces the digest; a strict `[0-9a-f]{64}` match is both validation and path guard; unsigned bundle → honest 404 "nothing to verify"), and `GET /api/verify/key` serves the signer's SPKI PEM (`rampscan.pub`). The copy-paste invocation stopped being generic: `ProjectionSettings` grew optional `verifyCommand`, stamped by serve as `pnpm rampscan verify <digest> --ledger <real dir> --keys <real dir>` — the page quotes the serve's OWN record, never a guessed default (settings is one JSON field; no PB schema change, older meta rows fall back to the bare command). Evidence page: "Verify this yourself" section (what-the-envelope-is prose, download DSSE bundle + download public key buttons — authorized fetch → blob, since a bare `<a href>` carries no PB token; unsigned bundles get a disabled button that says why) and the header now surfaces what the bundle already carries: scanned commit, dataset pin, tool versions, straight from the signed predicate (the predicate has no separate extractor-version field — `tool_versions` is what it carries, shown honestly, nothing invented). The `pnpm rampscan verify` line moved from the Reproduce block into the verify section (real dirs beat the bare digest form). Smoke grew test 6 — the strongest assertion in the suite: sign in → flagship evidence → download both artifacts → verify them with **node:crypto ONLY** (sha256(payload) === digest; payload contains the flagship's statement; ECDSA P-256 over the DSSE PAE against the downloaded PEM — zero rampscan code in the check), then run the page's own displayed invocation verbatim (`pnpm rampscan` prefix → the package script's real tsx entry) and assert "signature ok" + "payload  ok". Tests: smoke 6/6 cold (fresh .smoke + .next-smoke, 5.3m), vitest 291/291, root + console typecheck clean. | The envelope is cosign's attestation envelope format (ECDSA P-256/SHA-256, in-toto payload) — independent verification needs no rampscan, which is the point; the smoke proves it with node:crypto. Next action: I3c (scoping register — approved AND rejected proposals with justifications, identities, signature status; the `proposals` collection and scoping bundles already carry the record). |
| 2026-08-14 | I3a | The control register page — the auditor's landing view: `/controls` renders BOTH rollup registers (Controls / KSIs tabs) straight from the I1a projections (`controls`/`ksis` collections — no new fold, no new collections, nothing extra for `rebuild` to prove). Each rollup row: state pill, id, repo, and the attributable coverage phrase — "N of M mapped recipes evidenced" plus non-zero violated/unevidenced/n-a count pills read directly from the projector's `counts` (computed, never typed; a recount from the rows agrees by I1a's construction). Expanding a row joins the mapped recipes LIVE from the `registers` collection — the sub-rows are the coverage board's rows, never a copy (a recipe the register projection lacks renders "not in register", never a guessed state) — each with its verdict pill, freshness age, and a link to its evidence bundle. **Every hop, both directions**: control → recipe (expansion) → bundle (sub-row link); and back, the evidence page's ksi/control ids and the board's KSI/control cells are now links to `/controls?reg=…&id=…`, which deep-links the rollup row expanded + highlighted (`useSearchParams` under a Suspense boundary for the static prerender; board cell links stopPropagation so the row's evidence click survives). New `RollupRecord` type in `lib/types.ts` mirrors the projector's record shape (`rollup_id` — "id" collides with PB's record id). Smoke grew test 5 (ground rule 5): ra-5 rolls up violated (the flagship's verdict wins the precedence) → expand → flagship sub-row → click → `/evidence/<64hex>` → click `ra-5` on the bundle → back at `/controls?reg=controls&id=ra-5` with the highlighted row violated and the flagship already visible. Tests: smoke 5/5 cold (fresh .smoke + .next-smoke, 6.1m), vitest 291/291 untouched, root + console typecheck clean. | Found en route: the first cold smoke run of the day failed the PRE-EXISTING evidence test — under load (vitest + two dev servers running alongside) the first client-side hit of `/evidence/[digest]` sat behind next dev's compile-on-first-hit longer than the 15s expect budget; the two navigations that pay that compile now wait 45s and the per-test cap is 120s (the failure was the compile, not the console — the same route passed 4 tests later, already compiled). Next action: I3b (verify-this-yourself block — the evidence page already renders the `rampscan verify` command; I3b adds raw DSSE bundle + public key download and lifts dataset pin / tool versions / scanned commit into the page header). |
| 2026-08-14 | I2e | The first Playwright smoke (ground rule 5) — the console's zero-automated-tests era ends, and Phase I2 exits complete. Four tests against a REAL world, nothing canned: `e2e/smoke-server.mjs` builds the fixture, scans it into `e2e/.smoke` (ledger/keys/out), gates on the flagship verdict (not-violated → the tools are missing; fail THERE with a `pnpm doctor` hint, not confusingly in a board test), and only then starts the same `rampscan serve` an operator runs — Playwright's webServer command IS that script, because Playwright starts the webServer BEFORE global setup runs (found the hard way: a setup-file `rm -rf` deleted the scratch ledger out from under a just-started serve, the fs.watch died with the directory, and the re-fold truncated the projection to zero mid-test). The four: signed-out bounces to /login and a demo identity lands on the board; the board renders the fixture scan's registers with the flagship (`no-critical-reachable-advisories`) violated + I2c pointer sub-row + the I2b strip saying "no daemon" out loud (the I2 exit sentence, asserted); evidence detail shows failing assertions and the `src/index.js » lodash/merge` call path; the action queue renders ranked — labels mapped to the tier order and asserted non-decreasing, with the scan's born-violated events present. Isolation earned, not assumed: serve grew `--pb-data` (scratch PocketBase data dir — the default would have handed the smoke the dev console's users and PENDING PROPOSALS, which live nowhere but that dir), and the smoke's next dev runs in its own `NEXT_DIST_DIR=.next-smoke` because NEXT_PUBLIC_* is inlined at compile time and .next's webpack cache does NOT invalidate on env change — a dev serve and the smoke sharing .next hand each other stale chunks pointing at the wrong PocketBase (the smoke's first failure mode: sign-in against a PocketBase that wasn't there). CI: `.github/workflows/smoke.yml`, the repo's first workflow — pnpm install, `pnpm fetch-pocketbase`, the four core tool binaries curl-pinned to the manifest's versions, chromium, `pnpm smoke`; semgrep/checkov/spectral resolve through their pinned Docker images (the collectors' documented fallback, same shape as the validated local run). The workflow practices what the recipes preach: actions SHA-pinned (`ci-actions-pinned` polices this file on the next self-scan), `permissions: contents: read` (checkov). Tests: smoke 4/4 green cold (fresh .smoke + .next-smoke, ~4 min) and warm (~26 s); vitest 291/291 untouched; root + console typecheck clean. | Deliberate scope: the smoke asserts nothing about recipes whose tools may be absent in CI — they go honestly unevidenced, which is the design working. `reuseExistingServer` locally only (ports are pinned, so anything on 3098 is a prepared smoke world; CI always cold). This workflow is NOT the deferred engine-track CI (collector families) — it is the console smoke, though its skeleton is what that track will extend. Self-scan note: the repo now HAS a workflow, so `ci-workflow-present`-shaped rows may flip on the next self-scan; `tests-in-ci` may still call `pnpm smoke` unrecognized — honest either way. Next action: Phase I3 (auditor lens — I3a control register page), or the engine track's Phase H CI wiring if it resumes first. |
| 2026-08-14 | I2d | The "since baseline" drift toggle: the board diffed against a chosen prior scan (default: previous), riding the as-of fold from I1b. The mechanism: `scanInstants` (a "scan" IS a distinct evidence predicate timestamp — the join stamps every bundle of a run with one clock, so scan enumeration is exact, no run-id bookkeeping), `resolveBaseline` (`previous` = second-newest scan at or before the current as-of; refuses honestly when the ledger records fewer than two scans), and `diffRegisters` — a pure STATE-level diff of two folds' register rows (refreshed evidence with a held state is "unchanged"; bundle movement stays the drift view's story), classifier total over every (from,to) pair including appear/remove and pinned by an exhaustive matrix test so no transition can fall through unnamed; kinds severity-sorted (newly-violated > evidence-lapsed > unscoped > removed > appeared > scoped > newly-evidenced > resolved), newly-violated changes carry the current row's I2c fix pointers + violated-streak fields. **One hand computes for both surfaces**: `computeBoardDiff` (@rampscan/cli) = ledger list + recipes + two `foldEntries` + `diffRegisters`; `rampscan board --since previous\|<iso>` leads with the rendered diff (composes with `--as-of`, baseline ≥ as-of is an honest error), and the console's read-only GET `/api/board/diff` (auth-checked like the decide route, structurally unable to write) returns the same outcome — terminal and browser can never disagree. Console: "since baseline" toggle in the board filters + prior-scan picker (all scans but the newest), diff strip with severity-colored count pills sharing KIND phrasing with the row badges, per-row "was X"/"new row" chips, removed cells listed in the strip (they have no row to badge), "changed only" filter; refetches keyed on meta.projected_at so badges never describe a stale board. Found and fixed en route: `new URL("…", import.meta.url)` at module scope in collectors (semgrep rules path, tools.json manifest) — webpack rewrites that pattern into an asset reference that crashes at module load, and since the cli barrel pulls collectors, it had silently 500'd EVERY console server route since Phase H (the approve route included; 405s again now — the fix is `join(dirname(fileURLToPath(import.meta.url)))`, which webpack leaves alone). Tests: 291/291 (14 new: exhaustive classifier matrix, scanInstants dedup/scoping-blind, resolveBaseline previous/capped/refusals, fold-to-fold diff incl. cross-recipe anchor-kill lapse + pointer riding + determinism + new-repo appearance; renderBoardDiff pinned). Root + console typecheck clean. Live smoke: CLI `--since previous` says "no register moved (75 cells held)" honestly (last two scans folded identical), `--since 10:00` shows 31 newly violated + 29 appeared; console route 401s signed-out, 400s garbage, agrees with the CLI signed-in; browser: toggle → strip → early baseline → colored pills + badges + changed-only → toggle off restores, zero console errors. | Design decision of record: the diff is computed fresh per request from the LEDGER (two folds), never persisted — no new collections, nothing to drift from the record, `rebuild` has nothing extra to prove. The baseline picker deliberately lists scan instants only (I2d); arbitrary-instant as-of UI stays I3d. Next action: I2e (the first Playwright smoke, ground rule 5) — or the Phase H CI wiring if the engine track resumes first. |
| 2026-08-14 | I2c | Fix pointers on violation rows: an operator reading a violated row now sees where the violation lives without a context switch. The gap the regex hid: the console was SCRAPING call paths out of the assertion's prose `detail` (one example row, truncated at 400 chars) — the full offender list never left the scan. Now `evaluateAssertion` attaches structured `offenders` (first 5 failing rows reduced to `file`/`line`/`check`/`call_path`) + `offender_count` to failing assertions, extraction pinned by test to every real producer's row shape (sast-gate, semgrep, reachability where `path` IS the call path — » is the discriminator, gitleaks, checkov, spectral, repo-facts usesRows, graph routes; a renamed field breaks `core/test/assert.test.ts` instead of silently losing pointers, and `sast-gate.test.ts` runs the extractor over the gate's LIVE rows). Collectors state their own `reproduce` command on the run result; it rides `RecipeResult` → predicate → evidence page (never typed console-side). **Identity deliberately untouched**: `offenders`/`offender_count`/`reproduce` are excluded from `sameEvidence` (they restate what `detail` witnesses), so NOTHING re-keys — pre-I2c bundles survive and their rows carry no pointers until real drift re-keys them (verified on the live self-scan ledger: old cells keep no-pointers honestly, fresh scans carry `fixtures/build-vulnerable-app.mjs:144 aws-access-token` etc.). Fold: violated `RegisterRow`s gain `pointers` (deduped across failing assertions, capped 5) and `introducedAt`/`introducingCommit` — the current violated streak walked back through the bundle chain (breaks on a clean bundle, spans evidence gaps; "first SCANNED commit the violation appeared at", said honestly — this works even for pre-I2c bundles since it needs only the chain). SQLite columns + PB fields for the three; `ensureCollection` now syncs ADDITIVELY (missing spec fields are PATCHed onto existing collections — without this PB silently drops unknown fields on create for every deployment older than the spec; additive only, removals stay hand-written migrations; e2e-tested against real PB). Console: board violated rows get a pointer sub-row (pointers + "violating since X · first seen at commit Y", scoping-sub-row pattern), queue tier-2 detail leads with `pointerSummary` + introducing commit (shared `lib/pointers.ts` phrasing so board and queue never disagree), evidence page renders structured offenders per assertion with honest "+N more — the artifact carries them all" (regex scrape demoted to pre-I2c fallback), reproduce block shows the collector's own command. Tests: 277/277 (25 new), root + console typecheck clean. Live smoke: self-scan → scan-result carries offenders+reproduce, `rampscan rebuild` reads back identical over the 63-statement mixed-shape ledger, projection DB shows streak on old bundles and no invented pointers. | Design decision of record: pointers ride the REGISTER ROW (fold-side), not a console-side bundle join — one hand computes, recount agrees, zero extra fetches. Pointer-less failing rows (e.g. `{workflow_count: 0}` summaries) keep `offender_count` but attach no pointers — never `{}`. Existing consoles need no migration file: `serve`'s `ensureCollection` grows `registers` in place on next start (PB will auto-write its own migration under pb_migrations when it does; commit it when it appears). Next action: I2d (the "since baseline" drift toggle — the as-of fold from I1b is the mechanism). |
| 2026-08-14 | I2b | The daemon status strip: the board header now says whether the machine that keeps it honest is running. The gap the plan wording hid: quiet ticks wrote NOTHING, so a healthy-but-quiet daemon and a dead one were indistinguishable in the record — the scheduler now emits a `tick` heartbeat per assessment (including quiet ones), carrying the cadence outlook computed from the real assessment (`nextScanDueAt` = oldest bundle + scan-at fraction, `scanDue`/`reason`, `checkIntervalMs`, `windowMs`) so "next expected action" is computed by the daemon, never re-derived. **Deliberate deviation from "source: daemon-events.jsonl"**: the heartbeat goes to a `daemon-status.json` SNAPSHOT the daemon overwrites each tick — one tick per 5-min interval appended to the events file would drown the mirror's 500-line tail in ~41 quiet hours and push a standing divergence off the queue; latest state lives in the status file, what happened stays in the events file. `serve` mirrors it into `daemon_status` (at most one row, replaced wholesale; missing file → empty mirror — a leftover heartbeat from a dead run never masquerades as a live daemon; half-written file keeps the prior row, the watcher re-fires). Console: `lib/status.ts` (`deriveDaemonStrip`: alive ≤ `STALE_AFTER_INTERVALS`(=2) × the interval THE DAEMON DECLARED in its heartbeat; a heartbeat missing its interval fails toward stale, never reassurance; no heartbeat row → "no-daemon", said not guessed) + `standingDivergences` extracted there and shared with the queue's tier 0 so strip and queue can never disagree about the report; `components/DaemonStrip.tsx` on the board header — alive (green, tick age + last scan + next action), STALE (red loud banner), no-daemon (honest muted). Tests: scheduler tick pins (one heartbeat per assessment incl. quiet, outlook math, lands before the scan decision like the warnings), `mirrorDaemonStatus` pins (replace-wholesale/missing-clears/half-written-keeps), `cli/test/daemon-strip.test.ts` loads the console copy by path (twin posture), daemon e2e pins the snapshot holds exactly the LAST tick and ticks stay OUT of daemon-events.jsonl. `pnpm test` 252/252, root + console typecheck clean. Live smoke: serve mirrored a status file into `daemon_status` (anonymous write 403), overwrite replaced the row, delete cleared it; in the browser the strip rendered alive, flipped to the red STALE banner ~4s after the heartbeat aged past the threshold (PocketBase realtime, no reload), and fell to "no daemon heartbeat" when the file was deleted. | `pb_migrations/1786685119_created_daemon_status.js` rides this commit. Restart any running `rampscan serve` AND any running `rampscan daemon` (the daemon must emit heartbeats before the strip can call it alive — an old daemon reads as no-daemon/stale, which is the honest reading). Next action: I2c (fix pointers on violation rows — semgrep file/line + reachability call paths already ride the artifacts). |
| 2026-08-14 | I2a | The action queue: `console/web/lib/queue.ts` — a pure derivation over the projection collections + the daemon's event stream, computed at RENDER time (the expiring tier moves with the clock; a stored queue would rot between projections), pointing at register rows but structurally unable to move them. Ranking of record: divergence > expiring-before-next-cadence > new violation > actionable unevidenced. The plumbing the divergence and skip tiers needed (shared with I2b): `daemon_events` PocketBase collection (console-readable, superuser-written — anonymous write 403s, verified live) + `packages/cli/src/events.ts`, a tail of `daemon-events.jsonl` into any sink — the FILE stays the record, the mirror rebuilds from its newest 500 lines on every serve start, a shrunken file triggers full re-ingest never a merge; `rampscan serve` gained `outDir` and runs the tail. The `scan-recorded` daemon event now carries the scan's unevidenced rows with reasons — unevidenced rows never reach the ledger (nothing to attest), so the skip reason rides the event stream, and `classifySkip` tells fixable (tool missing → doctor hint, collector crashed) from honest ("nothing IaC-shaped") — honest skips stay OFF the queue. A "new" violation is an ordering, not a filter: every live violated row queues, stamped with the drift event that made it violated, newest movement first. `/queue` page + nav; `EXPIRING_AT_FRACTION` named in console mvx (twin test still pins it). Tests: `cli/test/action-queue.test.ts` loads the console's queue copy by path (mvx-twin posture) and pins `classifySkip` against the REAL reason producers (`absentReason` from collectors, the join's wrapping) so a reworded reason breaks the test instead of silently falling off the queue; `cli/test/events.test.ts` covers backfill/tailLimit/partial-line/truncation/missing-file. `pnpm test` 236/236, root + console typecheck clean. Live smoke: serve mirrored the repo's real 5-event file, watcher ingested an appended line in ~3s. | The four pb_migrations for controls/gaps/ksis (I1 collections, first created by this serve run) + daemon_events ride this commit. Restart any running `rampscan serve` to pick up the tail and the queue. Next action: I2b (daemon status strip — the `daemon_events` mirror it needs already exists). |
| 2026-08-14 | I1 | Full substrate: `Projection` gains `controls`/`ksis` (`RollupRow`: rolled-up verdict + attributable counts, computed from register rows so a recount always agrees), `gaps` (`CadenceGap`: chain × MVX window, ongoing tail ends at `projectedAt` — never a wall clock), and `FoldOptions.asOf`/`windowMs`. SQLite tables + PocketBase collections (`controls`, `ksis`, `gaps`) with drop-and-refill and lossless read-back — `rampscan rebuild` proves it over the real ledger. CLI: `rampscan board --as-of <iso>`; serve/daemon/rebuild folds now carry `windowMs` so persisted projections include the gap history. Tests: `projector/test/substrate.test.ts` (10), `scheduler/test/mvx-twin.test.ts` (3, loads the console copy by path — a workspace import would break `tsc --build`), Phase I1 exit block in `cli/test/ledger.e2e.test.ts` (as-of between two real scans reproduces scan 1's projection byte-for-byte via `canonicalJson`; control recount from rows). `pnpm test` 219/219, `pnpm typecheck` clean. | PocketBase rollup field is `rollup_id` ("id" collides with PB's record id); SQLite gap columns `gap_start`/`gap_end` ("end" is a keyword). Exit-test determinism: `asOf` filters on predicate timestamps and `projectedAt` is caller-supplied, so the projection object itself never carries a wall-clock read. Next action: I2a (action queue page). |
| 2026-08-14 | — | Track created: this document. Decisions from the persona review: one console two lenses; registers stay the record (workflow state console-writable, never projected); auditor lens read-only + export-heavy; sequencing substrate → operator quick wins → auditor lens (auditor-only postpones daemon visibility and would demo beautiful stale evidence; operator-only postpones the differentiator). Known debts this track inherits: console has zero automated tests; `cadence.test.ts` asserts re-stated mvx thresholds without importing the console copy. | No code yet. Next action: I1a (control register projection in `packages/projector`). |
