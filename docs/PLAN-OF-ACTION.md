# rampscan — plan of action (local development)

**Status:** working checklist — the executable layer over `docs/IMPLEMENTATION-PLAN.md` (M0–M5, complete) and `docs/IMPLEMENTATION-PLAN-REMAINING.md` (everything after Phase H — task breakdown, decisions, exit tests). Those plans own scope; this document owns task order and current status, and gets updated as work lands. On any scope dispute, the implementation plan wins; on any architecture dispute, `docs/SPEC.md` wins.
**Date started:** 2026-08-13
**Mode:** everything local — no AWS, no Terraform, no Bedrock. Every AWS dependency enters only as a port interface with a local adapter (plan §2).

---

## 0. Ground rules, active from the first commit

1. **Ports and adapters or it doesn't merge.** Any code touching storage, signing, execution, scheduling, or repo access goes through a `packages/core` interface. No direct `fs`/`child_process` calls from business logic.
2. **`dataset_version` pin enforced at load.** The dataset client refuses to run on a mismatch. Dev mode reads `docs/context/ramprules/derived/`; never treat those copies as the source of truth.
3. **Append-only is enforced by the adapter, tested by a cheat.** The ledger test suite includes a test that *tries* to rewrite/delete and must fail.
4. **Computed, never typed.** Any number that appears in generated docs (`FRONTIER-PIPELINE.md`) comes from a run, not a keyboard — the ramprules house rule, inherited.
5. **Tool versions in every cache key and every bundle's provenance** from M1, exactly as the appliance will do it.
6. **Tests ride with the milestone.** vitest unit tests per package as it's built; the M1–M2 end-to-end test against `fixtures/vulnerable-app` is part of M2's exit, not a cleanup task.

---

## Phase A — environment and scaffold (before M0 counts)

- [x] A1. `git init` + first commit of the docs tree. Commit anchoring is the product's spine; the repo itself needs history from day one (M5 self-scan and gitleaks history scanning both need real commits).
- [x] A2. Toolchain check: Node 22, pnpm. Record exact versions in the session log below.
- [x] A3. pnpm workspace scaffold — only what M0 needs: `packages/schema`, `packages/dataset`, `packages/core`, `fixtures/vulnerable-app`. Shared `tsconfig`, vitest, Zod. No console/, no infra/, no empty placeholder packages.
- [x] A4. `rampscan doctor` (can be a stub script first): checks for syft, osv-scanner, grype, gitleaks, cosign; prints install hints (brew/apt/release download). Docker optional, as fallback runner only.

**Exit test:** `pnpm install && pnpm test` runs green on an empty-but-wired workspace; `git log` shows history.

---

## Phase B — M0: contracts (plan: 1–2 days)

Build order inside the milestone — schema first, because everything else imports it:

- [x] B1. `packages/schema` — **Finding** (code-graph shared schema + `ksi_ids`/`control_ids`), **PipelineRecipe** (field-for-field mirror of `aws-evidence.json`'s recipe shape: `ksi_ids`, `control_ids`, `evidence`, `collection` with `kind: "pipeline"`, `expected_output`, `assertions`, `cadence`, `automatable`, `notes`, `caveats` — sole rename `govcloud` → `caveats` — plus `anchor: commit`), **EvidenceBundle** (in-toto statement shape). All Zod, with round-trip tests.
- [x] B2. `packages/dataset` — loader over `docs/context/ramprules/derived/` (dev mode) with the `/api/*` pinned mode stubbed behind the same interface; `recipe(id)`, `controlsFor(ksi)`, `ksisFor(control)`; hard failure on `dataset_version` mismatch.
- [x] B3. `packages/core` — the six ports (`LedgerStore`, `Signer`, `Runner`, `Scheduler`, `RepoSource`, `Projector`) as interfaces; local adapters stubbed, not implemented.
- [x] B4. `fixtures/vulnerable-app` — the planted-fault toy repo: a planted secret (in history, not just HEAD), a vulnerable dependency, an unpinned CI action. Committed with its own git history inside the fixture. *Deviation: a nested `.git` cannot be committed, so the fixture is generated deterministically (fixed timestamps/identity → stable SHAs) by the committed `fixtures/build-vulnerable-app.mjs`; the generated repo is gitignored.*

**Exit test (plan §M0):** `pnpm test` green; a hand-written recipe round-trips through the schema; `ksisFor("ac-2.1")` answers correctly against the snapshot.

---

## Phase C — M1: scan → joined findings (plan: 3–4 days)

The thesis milestone — if the join doesn't produce honest verdicts here, stop and rethink before building storage.

- [x] C1. Collector manifest format + the `Runner` local adapter (spawn → parse JSON → Findings), Zod-enforced at the wrapper boundary.
- [x] C2. Collectors, in this order (cheapest first, each demoable alone):
  - [x] `repo-facts` (hand-rolled, no external tool — lockfile pinning, CI workflow presence/provenance) — proves the manifest shape without dependency risk
  - [x] `gitleaks` (secrets, full history)
  - [x] `syft` (SBOM/CycloneDX)
  - [x] `osv-scanner` (advisories against the SBOM)
  - [x] `grype` (container image from the repo's Dockerfile; graceful skip when absent)
- [x] C3. `recipes/pipeline/` starter set — ~12 hand-authored recipes covering what these collectors plus M4 can prove (SBOM-exists-and-fresh, no-secrets-in-history, lockfile-pinned-deps, CI-provenance-present, container-base-image-patched, no-critical-reachable-advisories, route-auth-coverage, …), each resolving to real KSI/control IDs (KSI-SVC/SCR/CNA clusters).
- [x] C4. The join: collector output × recipe assertions → per-recipe verdict (`evidenced | violated | unevidenced`) with artifacts + commit hash.
- [x] C5. CLI: `rampscan scan <path>` → three-register terminal summary + `scan-result.json`. (No installable bin yet: `pnpm rampscan scan <path>` via tsx.)

**Exit test (plan §M1):** scanning `~/Projects/ramprules.com/fedramp-rules-hub` yields real evidence rows *and* real unevidenced rows; scanning the fixture yields violations with artifacts; every verdict cites recipe/KSI/control IDs that resolve against the dataset.

---

## Phase D — M2: ledger, signing, honest death (plan: 2–3 days)

- [x] D1. `packages/ledger` local adapter: `ledger/objects/<sha256>` + `index.jsonl`; the cheating test (rule 3) lands with it.
- [x] D2. `packages/signer` local adapter: in-toto statement per bundle, DSSE/ECDSA-P256 keypair (cosign envelope format, node:crypto implementation); `rampscan verify <digest>` works offline.
- [x] D3. Anchor death in the projector: anchoring content hash changed at the scanned commit → edge marked `dead(anchor-drift)` with the killing commit.
- [x] D4. `packages/projector` v1: ledger → plain SQLite projection (coverage, verdict history, freshness per bundle). PocketBase waits for M3.
- [x] D5. The end-to-end test: full M1–M2 loop (two scans, touched file, anchor death, surviving signatures) in CI.

**Exit test (plan §M2):** two scans with a touched file in between → touched-path evidence dies, untouched evidence survives with its original signature, `verify` passes on old and new bundles, and the append-only cheat test fails to cheat.

---

## Phase E — M3: console (plan: 3–4 days)

- [x] E1. Vendor PocketBase (pinned version) under `console/pocketbase/`; projector becomes its only writer; `rampscan rebuild` proves projection ≡ ledger.
- [x] E2. `rampscan serve`: PocketBase + Next.js locally. Coverage board (three registers, filterable), row → evidence detail (artifacts, assertions, commit, signature, reproduce command).
- [x] E3. Clock view (bundle age vs class window from config, b=7d / c=3d; expiring-first) and drift view (died / born / verdict-flipped, with cause).
- [x] E4. First two-key write: `notApplicable` proposal → approver signs → ledger event → register updates via projection.

**Exit test (plan §M3):** the M2 demo runs visually end to end, and a `notApplicable` survives `rampscan rebuild` because it lives in the ledger.

---

## Phase F — M4: graph + reachability VEX (plan: 4–6 days, hardest)

Scope guard: TypeScript/JavaScript only; entry points config-overridable; the demo is one reachable + one unreachable vulnerable dep — not graph completeness.

- [x] F1. `packages/graph`: TypeScript compiler API (deviation from tree-sitter + scip-typescript — see session log) → `graph.db` (symbols, imports, calls, routes, dependency nodes; exact vs inferred per edge).
- [x] F2. `reaches(entrypoints, vulnerableSymbol)` recursive CTE; entry points from package.json bins/exports + declared routes, with `graph.entrypoints` override in `rampscan.config.json`.
- [x] F3. Advisory gating: reachable → `violated` with the call path as the artifact; unreachable → `evidenced` (not-affected), emitted as OpenVEX with justification.
- [x] F4. Route-auth coverage: route nodes + "every route reaches an auth check" CTE; turns the M1 recipe's assertion live.
- [x] F5. Fixture upgrade: one reachable and one unreachable vulnerable dependency, provably different. Console shows the path; VEX lands in `exports/`.

**Exit test (plan §M4):** rampscan proves the reachable/unreachable difference on the fixture — path shown for one, signed not-affected VEX for the other.

---

## Phase G — M5: the clock + self-scan (plan: 2–3 days)

- [x] G1. `packages/scheduler` local adapter: `rampscan daemon` (tick loop on the class cadence — deliberate deviation from node-cron, see session log), nearing-expiry warnings *before* the window closes.
- [x] G2. Incremental scans by content-hash dirty set + scheduled full scan that diffs against the incremental result and alerts on divergence.
- [x] G3. Self-scan: rampscan scans its own repo; `docs/FRONTIER-PIPELINE.md` v0 generated from real runs, numbers computed never typed.

**Exit test (plan §M5):** daemon runs unattended across a multi-day window, produces fresh evidence on cadence, flags one near-expiry, and the self-scan board is worth reading.

---

## Deferred by name (do not build these locally)

Terraform / Fargate / Step Functions · KMS + Object Lock · Bedrock / any model · the 121-control adjudication (starter set only) · multi-repo joins · GovCloud · React Flow canvas · webhooks.

*(un-deferred post-M5: the semgrep / checkov / spectral collector families — see Phase H below)*

---

## Phase H — post-M5 engine expansion: three collector families

Engine-first, by decision (2026-08-14): CI wiring waits; the graph stays a single-producer substrate that new collectors consume, never rebuild.

- [x] H1. `semgrep` collector — pure producer (posture of osv-scanner): vendored pinned ruleset (`packages/collectors/semgrep-rules.yaml`, never the registry), committed-tree honest (untracked+ignored excluded via git), normalized artifact `semgrep-results.json` (repo-relative paths, path-independent check ids, sorted — byte-stable across binary/Docker runtimes). Ruleset content folded into the cache salt (`cacheKeySalt`).
- [x] H2. `sast-reachability` gate — M4's flagship move generalized to code findings: semgrep-results × graph.db → gated rows; reachable ERROR → violated with the call path as the artifact, provably-unreachable → not_affected, no graph / no entry points / unknown file → counts as unknown. Recipe `no-reachable-dangerous-code`.
- [x] H3. `checkov` collector (config plane, no graph by design) — frameworks limited to what the committed tree contains (Dockerfile / GitHub workflows / Terraform), `--skip-download`, honest skip on nothing-IaC-shaped. Recipe `iac-baseline-clean`.
- [x] H4. `spectral` collector (config plane) — OpenAPI/Swagger documents by name in the committed tree, always rampscan's ruleset (`spectral:oas`), telemetry side-process bypassed via entrypoint override. Recipe `api-spec-lint-clean`.
- [x] H5. Fixture upgrade: the SAST edition of the reachable/unreachable pair (`src/render.js` eval required from the entry point vs `src/legacy-tools.js` eval imported by nothing) + invalid `openapi.yaml`; Dockerfile/workflow double as checkov targets.
- [x] H6. Graph 0.2.0 — workspace-aware import resolution (see session log: the self-scan caught a false not_affected).

**Exit test:** fixture scan → `no-reachable-dangerous-code` violated citing exactly the reachable eval with path `src/index.js » src/render.js` (the unreachable one gated not_affected), `iac-baseline-clean` violated (3 failed checks), `api-spec-lint-clean` violated (1 oas3-schema error); self-scan → `no-reachable-dangerous-code` evidenced, checkov/spectral honestly skipped. All pass — see FRONTIER-PIPELINE.md.

---

## Phase I — console personas (operator + auditor lenses)

Tracked in its own working checklist: **`docs/PLAN-OF-ACTION-CONSOLE.md`** (started 2026-08-14). One console, two lenses; substrate → operator queue → auditor register/verify/export.

---

## Phase J — observability: the run record + `/runs` (started 2026-08-15)

By decision (2026-08-15): ZAP/DAST is **parked** — runtime evidence is a new anchor class and a milestone of its own, not a rider (analysis kept in `docs/BRAINSTORM-DAST-OBSERVABILITY-AI-HELPER.md` §1). Observability goes first: it benefits every existing collector, it is the operator's answer to "what ran and what did it output", and today the board shows conclusions while nothing durable shows the machinery. The data mostly already exists at the Runner boundary (`toolVersion`, `exitCode`, skip reasons) and is currently discarded after the join. Plan detail: brainstorm doc §2 + §5.

- [x] J1. The run record, **ledger-first**: new statement kind `scan-run`, signed + appended once per scan — per collector: tool + resolved version, runtime (`binary(path)` | `docker(image, digest)` | absent), redacted argv, duration, exit code, artifacts (name + sha256 + bytes), cache hit/miss with key inputs, skip reason. Captured at the Runner boundary (single choke point, collectors untouched). Projector folds it into a `scan_runs` collection (projector-only-writer rules, like every projection); rebuild byte-equality and `rampscan verify` extend to it. Ledger-first by decision: the manifest is the *how* behind every bundle — auditors ask for exactly this — and a console-side collection would be the only operator-facing record that can't be verified or rebuilt. *(Landed with ONE capture point rather than the planned two: `exec()` in `support.ts` is the single door every collector process goes through, so journaling there is structurally complete — no collector can under-report by taking a shortcut, and `git` calls collectors make directly are recorded too. The journal rides an `AsyncLocalStorage` opened by a Runner decorator, deliberately NOT a field on the collector contract. Predicate deviation: `tools[]` is an array, because a collector resolves zero tools (repo-facts, graph) or several, and a flat `tool/runtime` pair would have had to invent one. argv redaction is an allowlist — flags, env-var NAMES, paths under roots this run owns, relative paths/globs, colon compounds, short lowercase vocabulary — everything else becomes `<redacted:N bytes>`, and `--flag=`/`ENV=` keep their name so the record says where to go look.)*
- [x] J2. `/runs` console page: scan timeline (scan instants × `scan_runs` — one row per scan: commit, trigger manual/daemon·incremental/daemon·full, duration, collector counts) → expandable per-collector table (runtime pill, version, duration, exit code, cache, artifact list). Skip counts **loud** on the timeline row — a silently-absent tool quietly turning a column unevidenced is today's most confusing failure mode. *(The three buckets PARTITION the dispatched set — ran + cache-hit + skipped is exactly the number dispatched — so the timeline row is reproducible by recounting the table it expands into; the evidence page's run-record summary was moved onto the same `runCounts` hand so the two surfaces cannot disagree about one run. `scanInstants` now folds run-record instants, the redefinition J1 deferred here: a scan that moved no evidence used to leave no trace, so `--since previous` meant "since the previous scan that moved evidence" while calling itself the previous scan.)*
- [x] J3. Board hop: "how was this produced?" link on every board row → `/runs?scan=…&collector=…`. Smallest diff, highest leverage — an unevidenced cell lands one click from the named skip reason. Ship the moment J2 renders. *(The sketch said "resolved from the row's live bundle's `run_id`", and reading the code first found NEITHER field was on the board: the run id lives in the bundle predicate, and nothing console-side mapped recipe → collector at all — so the unevidenced hop, the case that needs it most, had no bundle to read either from. Both became part of the fold's EXISTING catalog join instead: `collector` from the catalog exactly as `ksi_ids`/`control_ids` do, `runId` off the live predicate beside `commit`. The board still fetches no run data, so it stays folded from evidence and scoping alone. A recipe missing from the catalog names NO collector rather than one inferred from the bundle's tool names. Two shapes: evidenced/violated → `?scan=…` "how was this produced?"; unevidenced → `?repo=…` "why is this empty?", where no run produced the cell so `/runs` substitutes the repo's newest scan and SAYS so; `notApplicable` gets no hop, its provenance being the signed scoping event. Three honesty cases the sketch did not name: a scan outside the projection cap, a projection with no run records at all (the live ledger's actual state — run records began at J1, and 9 of the board's cited runs predate them), and a collector the target run never dispatched.)*
- [ ] J4. Artifact viewers: normalized artifacts rendered as tables (semgrep/osv/gitleaks/checkov/spectral results; graph route rows), raw download alongside in the evidence page's authorized-fetch posture — the operator finally *sees* what a tool said instead of inferring it from a verdict pill.
- [ ] J5. Provenance chain rendered end-to-end and clickable both ways (recipe → collector → tool@version → artifact digest → bundle digest) on evidence pages + run rows; tooling-health card on `/runs` (doctor's resolution, live + per-run historical); `rampscan tools` CLI (the static recipe ↔ collector ↔ tool ↔ pinned-image map — doctor answers "can it run", `tools` answers "who feeds whom", `/runs` answers "what happened").

**Exit test (Phase J):** fixture scan → `/runs` shows every collector's runtime/version/duration/artifacts and the signed `scan-run` verifies offline via `rampscan verify`; with one tool deliberately absent, the timeline row counts the skip loudly and the board's unevidenced cell hops to the named reason; `rebuild` reproduces `scan_runs` byte-for-byte; the provenance chain renders end-to-end for `no-reachable-dangerous-code`.

---

## Phase K — comprehension layer (queued behind J)

- [ ] K1. Plain-language layer, **no AI**: `plain` field on all recipes (one operator-English paragraph — what this checks, what a violation means in practice) rendered on board rows + evidence pages; glossary-on-hover for the jargon layer (KSI, DSSE, anchor, verdict classes, MVX window, not_affected, two-key); guided empty states wired to J1's skip reasons.

BYOK AI helper (would-be K2) and DAST each remain their **own go/no-go decision** after J + K1 land — brainstorm doc §3 and §1.

---

## Session log

Update this table as work lands — newest first. "Phase" refers to this document's phases.

| 2026-08-15 | J3 | The board hop — one link per row from the conclusion to the machinery that produced it. **The plan's sketch could not have been built as written, and reading the code first is what found that**: it said "resolved from the row's live bundle's `run_id`", but `RegisterRow` carried `bundleDigest` and no run id (that lives in the bundle predicate), and **nothing console-side mapped recipe → collector at all** — `recipe.collection.collector` is catalog-only, and a bundle names TOOLS, not the collector that ran them. The unevidenced hop — the case the plan itself calls the one that needs it most — has no bundle to read either field from, so it was structurally impossible the sketched way. **Both fields became part of the fold's EXISTING catalog join** rather than a second lookup: `collector` arrives exactly as `ksiIds`/`controlIds`/`cadence` already do, and `runId` is lifted off the live predicate beside `commit` and `freshAsOf` — two columns through `registers` in sqlite and PocketBase, and `rampscan rebuild` on the live ledger folds 63 statements → 75 cells reading back byte-identical. No new route, no per-row bundle fetch, and **the board still fetches no run data whatsoever**: a board that queried the run log to draw itself would be partly a function of it, which is the standing J1 rule this had to stay clear of (re-pinned by a test). **The guessed collector was the tempting shortcut and is refused** — a recipe that fell out of the catalog names NO collector rather than one inferred from the bundle's tool names, and its row hops without the `&collector=` half; a wrong collector name on the one link an operator follows to find out why a cell is empty is worse than no name at all. **Two shapes, because a board row has two honest answers.** Evidenced/violated → `?scan=<run_id>&collector=…`, "how was this produced?" — THE run, exact. Unevidenced → `?repo=…&collector=…`, "why is this empty?" — no run produced that cell, which is what unevidenced MEANS, so `/runs` substitutes the newest recorded scan of the repo and says so in a notice rather than letting the substitution quietly read as the real thing. `notApplicable` gets **no hop at all**: a scoped-out cell was not produced by a run and is not missing because of one, and pointing at a scan there would imply a run is why it reads n/a — its provenance is the signed scoping event already on the row. **Three honesty cases the sketch never named, every one found by running it against real data:** a named scan absent from the capped projection now says WHICH fact applies — older than the cap, or (the live ledger's actual state) *no scan has appended a run record here yet*, since run records began at J1 and 9 of the board's 9 cited runs predate them, so on today's ledger every hop lands on that branch; and a collector the target run never dispatched says "never dispatched" instead of promising a highlight it will not draw, which is a different fact from "it ran and skipped". `stopPropagation` like every in-row link since I3a, and the row's own evidence click still works (the smoke re-walks it after using the hop). The link rides the control register's recipe sub-rows too, and is suppressed under a historical fold — `/runs` reads the LIVE projection, so a hop from an as-of board would leave the chosen instant without saying so (I3d's propose-N/A precedent). Tests: vitest **381/381** (13 new — 6 fold tests over the two joined fields: per-row collector, catalog-miss naming none, `runId` following the LIVE bundle and never a superseded one, unevidenced and scoped-out carrying none, run records still introducing no cell; the sqlite round trip re-pinned with a distinct collector per row so a dropped or smeared column would show; and 7 `runHop` twin tests in the export-csv/mvx-twin posture covering both shapes, both null cases, and URL encoding of a repo name containing a slash). Root + console typecheck clean. | Smoke **11/11 cold (2.7m)** — test 11 walks the real fixture: the flagship's hop resolving to `reachability` (the catalog's collector, not a tool name), landing on the run the board named with that collector highlighted and NO missing-run notice; **every** evidenced/violated row carrying a hop, not just the flagship, because a board where some rows explain themselves and others do not is the failure mode this link exists to remove; the row's evidence click surviving the hop; and both unevidenced-arrival notices asserted against the real run record. Found en route: this fixture is fully tooled, so its board has no unevidenced row to click — the shape is pinned in the twin test and the LANDING is pinned here by driving the URL `runHop` produces, rather than leaving a conditional assertion that never fires. Also restored the `next-env.d.ts` `.next-smoke` stamp the Playwright teardown leaves behind (same as I3d). Next action: J4 (artifact viewers — normalized artifacts as tables, raw download in I3b's authorized-fetch posture, and a `GET /api/artifact?digest=` route that re-hashes before serving). |
| 2026-08-15 | J2 | `/runs` — the board shows conclusions; this page shows the machinery that produced them. One timeline row per recorded scan (when, commit, trigger, duration, collector counts, and the run record's own digest linking to its evidence page), expanding into the per-collector table: runtime pill, tool@version with the resolved path or the pinned image AND the repo digest it actually pointed at, duration, exit code, findings, artifacts with their sha256, cache state, and the argv of every invocation with the allowlist's `<redacted:N bytes>` markers left visible rather than hidden. Read straight from the projected `scan_runs` collection — realtime like every other projection view, **no new server route**, because the projection is already console-readable. **The counts PARTITION the dispatched set**: ran + cache-hit + skipped is exactly the number dispatched, so a cache hit is never counted as having run (nothing was spawned) and a reader who recounts the expanded table always reproduces the timeline row — smoke 10 does exactly that, parsing the row's own claim and then counting collector rows and runtime pills against it, because "the summary agrees with the table" is the only version of this page worth shipping. The evidence page's run-record summary, shipped in J1 with overlapping buckets, was moved onto the same `runCounts` hand: two surfaces describing one run must not disagree. Skip counts are **loud** (`N SKIPPED ⚠`) and a clean run says `0 skipped` out loud rather than leaving the reader to infer it from silence; skipped collectors sort to the TOP of the table, because a skip buried at row 9 of 11 is the failure mode this page exists to fix. Precedence in the runtime pill is deliberate — skipped beats cache-hit beats how the tools resolved — and a collector that asked for no external tool reads `pure` ("no external tool — this collector reads the repo itself"), never `absent`: that distinction is the difference between a fact about a collector and a missing binary. The page is deep-linkable at `/runs?scan=<run_id>&collector=<name>` (arrives expanded, the named collector highlighted, its argv already open) — that is the address J3's board hop will point at, built and pinned now. **Also landed here: the `scanInstants` redefinition J1 deferred to J2** — run-record instants now count as scan instants. A run carries the same clock as the bundles it produced, so an evidence-moving scan is untouched; what changes is the scan that moved NOTHING, which used to leave no trace at all, so `--since previous` and I3d's as-of quick-picks meant "since the previous scan that moved evidence" while calling themselves the previous scan. They now mean the literal thing they say, and an empty diff because nothing changed is a true answer rather than a missing one. What this page may never do, enforced by having no way to do it: compute a verdict, a register state, or a coverage number — `lib/runs.ts` is counts and labels only, and the board stays folded from evidence and scoping alone. Tests: vitest **368/368** (20 new — the twin test over the console's own `lib/runs.ts` pinning the partition, the cache-hit-is-not-ran rule, a skipped collector whose cache says hit still counting once, pill precedence, `pure` vs `absent`, a tag never quoted as a digest, redaction counting, duration scaling and both orderings; plus two scanInstants tests: a run record beside its own evidence adds no second instant, and a scan that produced no evidence is still a scan). Root + console typecheck clean. | Smoke **10/10** (3.4m) — test 10 walks the fixture scan's real run record: one timeline row, `manual`, `0 skipped`, the row's claimed counts recounted against the expanded table and its runtime pills, `repo-facts` rendering `pure` with its artifact and "no external tool", `syft` rendering `binary` with `syft@1.51.0 · /home/sunny/.local/bin/syft`, `sbom.cdx.json` and 3 invocations, the deep link landing highlighted with argv open, and the digest hop reaching the run record's own evidence page (`scan run` pill, no verdict). Found en route: the first pass asserted `sbom.json` where the collector actually writes `sbom.cdx.json` — the smoke read the real record and said so. Next action: J3 (the board hop — one link per row to `/runs?scan=…&collector=…`, resolved from the row's live bundle's `run_id` or the newest run when the cell is unevidenced; an afternoon, and the highest-leverage piece in the phase). |
| 2026-08-15 | J1 | The run record — evidence says what was concluded; from here something durable says how the run that concluded it went. New third statement kind `https://rampscan.dev/scan-run/v1` in the `LedgerStatement` union with `isScanRun()` beside the two existing guards, signed and appended once per scan, **always-append and never deduplicated** (a run is unique by construction, and two identical-looking scans an hour apart are two facts, not one). **The capture point is ONE seam, not the two the plan sketched**: every process a collector starts goes through `exec()` in `support.ts`, so the journal records there — structurally complete, since there is no other door, and it picks up the `git ls-files` calls collectors make directly as a bonus; `resolveTool()` records how each tool resolved, `absent` included with the reason stated. The journal rides an **AsyncLocalStorage** opened by a Runner decorator (`createJournaledRunner`) rather than a field threaded through `CollectOutput` — routing telemetry through the collector contract would make every collector responsible for reporting its own provenance, precisely the arrangement where one quietly under-reports; here a collector is recorded whether it cooperates or not. Telemetry rides `RunResult`, so the scan cache persists and restores it: on a **cache hit** the recorded invocations are the ones of the run that PRODUCED the cached result, and `cache.state` is what tells a reader so — the record never claims a spawn that did not happen. Four decisions taken as recommended: signed (not unsigned in `out/`), **no bundle → run back-reference** (join on the `run_id` the evidence predicate already carries — re-keying every bundle to gain a link is the trade I2c declined), **docker image digests resolved and recorded** after the collector ran (the pinned image may only have been pulled by the invocation just watched; `null` + a stated reason when Docker cannot answer — this delivers the H phase's deferred digest pinning as a side effect), and the projection **capped at 200 runs** while the ledger keeps all. **argv redaction is an allowlist, deliberately**: flags, env-var NAMES (uppercase belongs left of an `=` and nowhere else), absolute paths under roots this run owns, relative paths/globs, colon compounds (`dir:/abs`, `host:/container:ro`, `image:tag`), and short lowercase vocabulary survive; everything else becomes `<redacted:N bytes>`, with `--flag=`/`ENV=` keeping their name so the record says WHERE something unrecordable was. A denylist of secret patterns is the tempting version and it is the one that eventually leaks — it fails open on every shape nobody thought of. Two standing rules enforced in code, not remembered: the fold derives register repos from **evidence and scoping alone** (a run record names a repo too, and letting it introduce cells would make the board partly a function of the run log), and the predicate carries no verdict, no register state and no coverage number — pinned by a test that greps the canonical bytes for all four state words. Ledger index rows keep their `recipe_id`/`verdict` slots EMPTY for a run record rather than inventing values, which is what stops `list({ recipeId })` ever handing one to an evidence chain. `rampscan verify` gained a scan-run branch; `Projection.scanRuns` folds, and SQLite + PocketBase (`scan_runs`, projector-only-writer) round-trip it byte-for-byte through `rampscan rebuild`. The evidence page learned the third kind — a run record landing in its `else` would have rendered as a notApplicable scoping, a wrong page rather than a rough one — and now shows the collector table with the named skip reason. Tests: vitest **347/347** (23 new: the allowlist pinned against `ghp_`/`AKIA`/`sk-proj-`/JWT/underscore/long-segment shapes, the journal capturing a collector that never cooperates, the full J1 exit test — one scan-run per scan, offline verify green, absent-tool runtime + reason, artifact digests equal to the bytes on disk, planted argv secret absent from statement AND canonical bytes AND base64 envelope payload, miss→hit cache states across two cached scans, rebuild byte-equality, and no run record touching a register cell). Root + console typecheck clean. | Live fixture scan, all 11 collectors real: 331s, 3 binaries with resolved PATHs, 3 Docker images with **real repo digests** (`semgrep/semgrep:1.173.0 → sha256:67319956da3dc…`), 5 redactions in the entire run (a `{{.Server.Version}}` template, three `HOME=/tmp` before the env-name fix, one `github_actions`) — argv reads like a shell transcript. `rampscan verify <run-digest>` green offline; `rebuild` on the live ledger: 16 statements → 15 register cells (the run record added none, as designed) and sqlite byte-identical. Deferred deliberately: `scanInstants` still enumerates **evidence** timestamps only, so `--since previous` and the as-of quick-picks are unchanged by this commit — folding run records into "a scan instant" would silently redefine I2d/I3d behavior, and it belongs to J2 where `/runs` renders the timeline anyway. Next action: J2 (`/runs` page over the `scan_runs` collection — timeline row per scan with skip counts loud, expandable per-collector table; then J3's board hop, which is an afternoon). |
| 2026-08-15 | J/K (planning) | Brainstorm + decision session, no code: `docs/BRAINSTORM-DAST-OBSERVABILITY-AI-HELPER.md` written (DAST/ZAP analysis, tool-run observability, BYOK AI helper). Decisions: **ZAP/DAST parked** (runtime evidence = a new anchor class = its own milestone; Mode A self-launch analysis kept for when it's picked up); **observability promoted to Phase J** (run record ledger-first as a `scan-run` statement — the how behind every bundle, verifiable and rebuildable like everything else — then `/runs`, the board hop, artifact viewers, provenance chain); **plain-language layer promoted to K1** (no AI); BYOK helper and DAST each held at their own go/no-go after J+K1. | Next action: J1 (`scan-run` statement kind + Runner-boundary capture). |
| 2026-08-14 | H (engine expansion) | Three collector families, engine-first (user decision: CI integrations wait, webhooks out). The organizing principle: **the graph is a substrate, not a collector family** — one extractor owns the code's shape, new collectors consume it. `semgrep` (producer, vendored pinned ruleset — the registry resolves over the network at scan time, so two runs of one commit could see different rules; ruleset content salts the cache via `cacheKeySalt`) + **`sast-reachability`** (the flagship move generalized: semgrep-results × graph.db, reachable ERROR → violated with the call path as the artifact, proven-unreachable → not_affected, everything else counts as unknown). `checkov` + `spectral` (config plane, no graph BY DESIGN — a Dockerfile has no call path): committed-tree framework/document detection, honest skip on nothing-shaped repos, `--skip-download` / entrypoint-override so neither phones home. Docker images pinned (semgrep 1.173.0, checkov 3.3.11, spectral 6.16.3); ToolSpec gained `entrypoint` (semgrep ships none; spectral's launches telemetry). Fixture: the SAST edition of the reachable/unreachable pair + invalid openapi.yaml. **The self-scan earned its keep AGAIN, catching two defects in one run**: (1) rule precision — `$X.exec(...)` matched RegExp.exec, sqlite db.exec, and rampscan's own execFile wrapper → 10 false ERRORs; the receiver must now actually be child_process. (2) **A false not_affected — the exact claim the architecture promises never to emit**: all 10 were "unreachable" because monorepo imports (`@rampscan/collectors`) dead-ended on dependency nodes, so the file-level walk never crossed package boundaries — files that run on EVERY scan counted as beyond every entry point. Fixed at the extractor: **graph 0.2.0, workspace-aware import resolution** (package.json name→entry-file map from the committed tree; workspace imports land on the package's entry source file, exact). `pnpm test`: 203/203. | Fixture scan (all collectors live via Docker/binaries): 2 evidenced / 13 violated / 0 unevidenced — the reachable eval violated with path `src/index.js » src/render.js`, the identical eval in the never-imported file gated not_affected (count 1, not 2); checkov 44 passed / 3 failed (unpinned action, no USER, no HEALTHCHECK); spectral exactly one error (missing `info`). Self-scan: 4 evidenced / 5 violated / 6 unevidenced — `no-reachable-dangerous-code` evidenced with ZERO semgrep hits (post-fix, not zero-by-false-gating), checkov/spectral skipped honestly (nothing IaC- or API-shaped committed). FRONTIER-PIPELINE.md regenerated: 8/11 KSIs, 15/21 controls. Console: evidence detail now surfaces `call_path` rows. Semgrep leaves one honest wart: a PartialParsing warn on support.ts (recorded as error_count in the artifact). |
| 2026-08-13 | G (M5) dogfood | The self-scan closed the loop and earned its keep: pointed at its own repo, rampscan's first board was WRONG in two honest-to-catch ways. (1) The graph collector walked the working tree, and syft's static exclude list missed generated content — the gitignored, test-generated `fixtures/vulnerable-app/` leaked its planted lodash/minimist lockfile into the self-SBOM and its `/health` route onto the self-board, i.e. uncommitted content inside commit-anchored evidence. Fixed at the principle: **collectors read the COMMITTED tree** — graph lists files via `git ls-tree` (fs-walk fallback for non-git unit-test roots; `ls-tree` from a subdir workspace scopes and relativizes correctly), syft's excludes are computed from `git ls-files --others --directory` (untracked + ignored), static names kept as fallback. (2) The daemon smoke run re-scanned a freshly-evidenced repo because `scan .` recorded repo identity `"."` while the daemon resolved it — evidence identity split by spelling; `scan()` now pins the workspace to the RESOLVED path. Post-fix board: 3 evidenced / 5 violated / 4 unevidenced — every row true: `no-critical-reachable-advisories` evidenced (entrypoint `packages/cli/src/main.ts` via the new root `rampscan.config.json`), `route-auth-coverage` honestly unevidenced (no routes, no vacuous pass), violations are real repo facts (no CI workflows in-repo yet, no CODEOWNERS, no dependency automation, and gitleaks correctly burning the planted fixture secret in the committed generator script at `edf4b6b`). `docs/FRONTIER-PIPELINE.md` v0 committed, generated by `rampscan report`, numbers computed never typed. Daemon smoke: watches, assesses the live ledger, stays quiet on fresh evidence. | The multi-day unattended window is now an operational step, not a code step: `pnpm rampscan daemon .` and leave it running. |
| 2026-08-13 | G (M5) | The clock runs itself. `packages/scheduler` — the sixth and last port's real local adapter: pure cadence math (`assessCadence`: scan due at 0.5 of the MVX window or on a moved HEAD or on no evidence; warn at 0.75, the console's "expiring" threshold; expired past 1.0) + a **setTimeout tick chain, not node-cron** — deliberate deviation, same house style as DSSE-without-cosign: cron expressions add nothing when the schedule is "check every N minutes, act on the clock math"; node-cron slots in behind the same port. Warnings emit BEFORE the scan decision each tick (a daemon that slept past 0.75 warns and then refreshes — never silence after the fact), once per bundle digest; a refresh mints a new digest and a new warning slot. **The freshness/identity tension resolved**: M2's evidence identity means unchanged evidence survives with its original timestamp — so continuously re-verified evidence would "expire" on the MVX clock. `scan()` gained `refreshOlderThanMs` (daemon sets window×0.5): identical evidence past it is **re-signed with a fresh timestamp** (prior dies `superseded` — an honest record of each re-verification); plain `rampscan scan` unchanged. **G2, the cache verifies itself**: `createCachingRunner` in core (a Runner decorator — collectors untouched), dirty-set by content hash read from `git ls-tree` blob hashes (no file I/O); per-collector `cacheScope` in the manifest — globs (repo-facts: exactly the files its checks read; grype: Dockerfiles; graph: ts/js+config), `@commit` (gitleaks, full history), `@tree` (syft), `@inputs` (osv-scanner, reachability — keyed on consumed artifact hashes). Key = collector + toolVersion + resolved scope + tools.json content hash salt (ground rule 5); skipped runs never cached (environmental); full mode bypasses reads but refreshes entries. Every `--full-every`th daemon scan runs full and `diffScanResults` (verdicts + assertion outcomes, tool-version changes as context) against the last incremental at the same commit → `divergence-report.json` + loud alert; this is also the designed corrective for what the cache cannot see (advisories published to OSV/grype's DB since the cached run, PATH tool upgraded in place). CLI: `rampscan daemon <path>` (`--cache`, `--check-interval`, `--full-every`; events logged + appended to `daemon-events.jsonl`), `rampscan report` (G3: `docs/FRONTIER-PIPELINE.md` from scan-result.json — registers, KSI/control coverage counts, theme rollup, tool provenance, skipped collectors; the generator recounts from rows, trusts nothing it can recompute). `packages/scheduler/src/mvx.ts` is the server-side twin of `console/web/lib/mvx.ts` (client bundle keeps its copy; thresholds agree by test). Core's Scheduler stub + its loud-stub test retired. `pnpm test`: 186/186. | Daemon e2e compresses the multi-day window to a fake clock (window=10s, injected `clock`): startup scan → quiet tick → cadence refresh with cache hits → quiet (clock reset by refresh) → scheduled full scan verifies clean cache → slept-past-0.75 tick warns BEFORE refreshing (remainingMs > 0) → poisoned cache entry rides one incremental scan and the next full scan catches exactly `lockfile-pinned-deps` diverging, report written → moved HEAD re-scans → `daemon-events.jsonl` holds every event. The wall-clock "unattended across a multi-day window" run is operational, not code: start `pnpm rampscan daemon <path>` and leave it. Self-scan: see FRONTIER-PIPELINE.md commit. **M5 exit test passes** in compressed form; prototype loop complete — all six ports real, all five milestones landed. |
| 2026-08-13 | F (M4) | The graph and the flagship join. `packages/graph`: extraction via the **TypeScript compiler API, not tree-sitter + scip-typescript** — deliberate deviation, taken to dodge the plan's own named risk (the SCIP rabbit hole: native bindings + an indexer install for what the prototype needs lexically). Same layering as the code-graph doc §3.2, honesty preserved per edge: `exact` = lexical fact (local decl, import binding, resolved relative path), `inferred` = unique name-match across the project (ambiguous names produce NO edge); scip-typescript slots in later behind the same node/edge schema. `graph.db` (node:sqlite): nodes (file/symbol/dependency/route), edges (imports/declares/exports/calls/handles), routes, meta (extractor version, commit, entry points, auth patterns). `reaches()` is a recursive CTE for the SET (the verdict); display paths are BFS (presentation). Two traversals, two deliberate approximation directions: advisory reachability walks every edge kind (over-approximate — `not_affected` only when even the loose walk can't reach the package); route→auth walks calls/handles only (under-approximate — a positive evidence claim needs an actual call chain, and rows say when it rests on an inferred edge). Entry points: `graph.entrypoints` config → package.json main/bin/exports → fallback; routes are always roots. Collectors: `graph` (graph.db artifact + route-auth observations + a finding per unauthed route; no routes → honestly unevidenced, never vacuously evidenced), `reachability` (osv-results.json × graph.db → gated rows; reachable CRITICAL/HIGH → finding with the call path as trace evidence; proven-unreachable → OpenVEX `not_affected` / `vulnerable_code_not_in_execute_path`; no graph or no entry points → M1 posture, rows marked `unknown`, VEX `under_investigation`); osv-scanner demoted to pure producer. Recipes: `no-critical-reachable-advisories` re-pointed at reachability (`count_eq 0 where severity in [CRITICAL,HIGH] and not_affected eq false` — unknown counts against you), `route-auth-coverage` assertion live. Fixture: minimist 1.2.5 (GHSA-xvch-5gv4-984h, CRITICAL) declared but never imported + hand-rolled express-shaped router (`/settings` behind requireAuth, `/health` bare — no real framework, so the lockfile carries only the two planted vulns). CLI copies openvex.json to `out/exports/`; console evidence page surfaces call paths from assertion details. `pnpm test`: 140/140. | **M4 exit test passes** live and in CI shape: fixture scan → `no-critical-reachable-advisories` violated citing lodash with path `src/index.js » lodash/merge`, minimist absent from findings, VEX statement `not_affected` with the entry-point impact statement, openvex.json a digest-pinned subject of the signed bundle (`rampscan verify` green on it); `route-auth-coverage` violated by exactly `GET /health`. Shortest-path note: the module-scope require IS the path (import = execution), so the flagship path runs through the import edge, not handleRequest. Next action: G1 (`packages/scheduler`, `rampscan daemon`). |
| 2026-08-13 | E (M3) | The board became visual and the two-key write landed. `packages/schema`: **ScopingEvent** (`https://rampscan.dev/scoping/v1` — subject = justification hash, predicate carries proposer + approver identities) and the **LedgerStatement** union; ledger/signer/verify widened to statements (one ledger, two statement kinds). `packages/projector` v2: fold now emits **registers** (projection × recipe set — unevidenced finally visible outside scan output; live scoping → `notApplicable`; live evidence outranks scoping), **drift events** (born / died / verdict-flipped / scoped, computed from chains), and gained a **PocketBase writer** (fetch-based admin client, v0.23+ collections API; drop-and-refill; projection collections created with `createRule/updateRule/deleteRule: null` so the projector's superuser is the ONLY writer — enforced by rules, tested by an anonymous-write-rejected test). PocketBase 0.39.10 pinned in `console/pocketbase/version.json` (sha256 per platform from the release's checksums.txt); `pnpm fetch-pocketbase` downloads + verifies, binary gitignored. CLI: **`rampscan rebuild`** (fold → SQLite → read back → require byte equality; exits nonzero on mismatch), **`rampscan serve`** (PB bootstrap: generated superuser 0600, users.role viewer/approver, demo accounts, proposals collection; fold → PB; fs.watch on `index.jsonl` re-projects on every ledger append; spawns Next.js dev with env-passed dirs), `recordScoping` (recipe resolved against catalog, dataset pin enforced, DSSE-signed, appended). `console/web`: Next.js 15 + PocketBase JS SDK, login (PB auth), **board** (4 register tabs, repo/KSI-theme/control-family filters, realtime), **evidence detail** (assertions, subjects with anchor-vs-artifact marking, signature, reproduce commands, raw statement), **clock** (age vs class window, expiring-first, b=7d/c=3d), **drift** (grouped by day with cause), **approvals** (propose from any account; approve/reject is approver-only via `/api/scoping/decide`, which verifies the PB token, signs the scoping with the approver identity in the predicate, appends to the LEDGER, then stamps the proposal). `pnpm test`: 114/114. | Next needed `resolve.extensionAlias` (workspace packages import NodeNext-style `./x.js` → x.ts). Proposals are console-writable by design (not projection); approved facts live only in the ledger. **M3 exit test passes** twice over: `packages/cli/test/scoping.e2e.test.ts` (scoping survives two rebuilds, register stays `notApplicable`, other 11 recipes stay visible unevidenced) and live in the browser — scan → board fills → propose → approve → register flips via watcher re-projection → touch Dockerfile → re-scan → exactly `container-runs-nonroot` + `container-base-image-patched` die with the killing commit named in drift. Next action: F1 (`packages/graph`: tree-sitter + scip-typescript → graph.db). |
| 2026-08-13 | tooling | Docker-first tool resolution: wrappers no longer spawn tools directly — `packages/collectors/src/tools.ts` resolves binary-on-PATH → pinned Docker image (`packages/collectors/tools.json`: syft 1.51.0, grype 0.117.0, gitleaks 8.24.3, osv-scanner 2.5.0) → skip with reason. **rampscan never installs anything on the host** (user decision: no install when Docker is possible; hence no binary downloader was built — doctor hints remain for the no-Docker case). Docker runs are path-mapped via `tool.mount(hostPath)` bind mounts, `-u uid:gid` so artifacts land user-owned, `HOME=/tmp` so git-in-container (gitleaks) behaves; grype under Docker mounts a persistent DB cache (`~/.cache/rampscan/grype-db` via `GRYPE_DB_CACHE_DIR`) so the ~700MB DB isn't re-pulled per scan. Docker tools report the bare pinned version — same string shape the binary reports — so evidence identity doesn't re-key across runtimes. doctor rewritten around resolution (binary / docker-image / MISSING). `pnpm test`: 97/97. | Live smoke with binaries hidden from PATH: gitleaks/syft/osv-scanner ran via pulled pinned images, identical verdicts (2/9/1) and identical `tool_versions` strings as the binary run; zero skipped collectors. Image-digest pinning (vs tags) deferred as appliance-level hardening, noted in tools.json. |
| 2026-08-13 | D (M2) | The output became a record: `packages/ledger` (content-addressed append-only dir — objects written `wx` + chmod 0444, `index.jsonl` append-only, `get()` re-hashes so out-of-band tampering throws; the cheat test chmods an object back and rewrites it, and is caught), `packages/signer` (DSSE envelope over the canonical in-toto statement, ECDSA P-256/SHA-256 via node:crypto — cosign's envelope format without the cosign binary; keys auto-generate at `--keys`, private key 0600), `packages/projector` (pure fold: per-recipe chains → predecessor dead as `superseded` or `anchor-drift` with killing commit; cross-recipe anchor death when another recipe's later bundle saw the same path change; SQLite writer via node:sqlite, drop-and-refill because the ledger is the record), `canonicalJson` in schema (one byte sequence per statement, shared by digest + signature). CLI: scan signs + appends each evidenced/violated row unless identical evidence already exists (**evidence identity = verdict + anchors + assertion outcomes + tool/dataset version, deliberately not artifact hashes** — collector artifacts aggregate every recipe, keying on them would kill everything on any change); `rampscan verify <digest>` (content + signature + payload-covers-this-bundle, offline); `rampscan board` (live/dead registers + `--db` SQLite). `pnpm test`: 90/90 green; typecheck clean. | Unevidenced rows never reach the ledger (no artifacts, nothing to attest) — the board's third register stays in scan output until M3 joins projection × recipe set. Live demo on the fixture: touching the Dockerfile killed exactly `container-runs-nonroot` + `container-base-image-patched` (anchor-drift, killing commit named), 9 bundles survived with original signatures, verify green on dead and live bundles. cosign now optional in doctor (independent envelope verification only). **M2 exit test passes** (`packages/cli/test/ledger.e2e.test.ts`). Next action: E1 (vendor PocketBase, projector as sole writer, `rampscan rebuild`). |

| Date | Phase | What landed | Notes / deviations |
|---|---|---|---|
| 2026-08-13 | C (M1) | Full vertical slice: `packages/collectors` (repo-facts, gitleaks, syft, osv-scanner, grype — each a `Collector` with manifest + Zod-parsed tool output), real `Runner`/`RepoSource` local adapters in core (runner is the Zod boundary; hashes + relativizes artifacts; feeds an inputs map so osv-scanner consumes syft's SBOM), assertion evaluator + the join in `packages/core` (aws-evidence semantics: row-wise ops vacuous on empty filtered sets, count ops over `where`-filtered rows), 12 recipes in `recipes/pipeline/` (KSI/control IDs validated against the dataset at scan time and in tests), `packages/cli` (`pnpm rampscan scan <path>` → three-register summary + `scan-result.json`; per-recipe rows carry assertions/artifacts/anchor_paths so M2 bundles are a re-keying). `pnpm test`: 66/66 green; typecheck clean. | Tools installed to `~/.local/bin` (gitleaks 8.24.3, syft 1.51.0, grype 0.117.0, osv-scanner 2.5.0). Fixture's planted secret changed: gitleaks allowlists AWS's documented example key (`AKIAIOSFODNN7EXAMPLE`), so the fixture now plants a non-allowlisted fake key — fixture SHAs moved. Fixture is built once per test run by vitest globalSetup (parallel per-file rebuilds raced). syft excludes uncommitted trees (`node_modules`, `.next`, `.git`, …): they aren't commit-anchored, and unexcluded the hub scan blew the 600s timeout. grype scans the final FROM's base image, not a locally built app image. **M1 exit tests pass**: fixture → 2 evidenced / 9 violated / 1 unevidenced (all four planted faults caught: 2 history secrets, lodash HIGH advisories, 2 unpinned actions + no provenance, node:16-alpine CRITICALs via grype); fedramp-rules-hub → 4 evidenced / 5 violated / 3 unevidenced — real evidence rows, real unevidenced rows, every verdict citing dataset-resolvable IDs. Next action: D1 (ledger local adapter + the cheating test). |
| 2026-08-13 | A + B (M0) | git history started; pnpm workspace (Node v22.22.2, pnpm 9.6.0, git 2.43.0); `packages/schema` (Finding / PipelineRecipe / EvidenceBundle / CollectorManifest, Zod, round-trip tests); `packages/dataset` (dev loader over `derived/`, pinned-mode stub, version pin hard-fails, `ksisFor("ac-2.1") → [KSI-IAM-JIT, KSI-IAM-SUS]` verified); `packages/core` (six ports + stubbed local adapters that reject loudly); `fixtures/build-vulnerable-app.mjs`; `scripts/doctor.mjs`. `pnpm test`: 17/17 green; `pnpm typecheck` clean. | Fixture is generated, not committed (nested `.git` can't be committed) — deterministic script committed instead, SHAs stable (HEAD `9352d78`). Doctor: syft/osv-scanner/grype/gitleaks/cosign absent on this machine, Docker present — install before C2. M0 exit tests pass. Next action: C1 (collector manifest + Runner local adapter). |
| 2026-08-13 | — | Docs phase: spec, implementation plan, architecture reference, this plan | No code yet. Next action: A1 (`git init`). |
