{
  "name": "@rampscan/runner",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "rampscan-runner: the client-deployed program that runs published AWS read recipes in the client's own account and hands the appliance a transcript. No runtime dependencies, no ledger key, no verdict.",
  "bin": {
    "rampscan-runner": "./src/main.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@rampscan/schema": "workspace:*"
  }
}
