{
  "id": "ci-provenance-present",
  "ksi_ids": [
    "KSI-SCR-MIT"
  ],
  "control_ids": [
    "si-7.1"
  ],
  "evidence": "At least one CI workflow generates build provenance or signed attestations tying artifacts to the commit that produced them",
  "collection": {
    "kind": "pipeline",
    "collector": "repo-facts"
  },
  "expected_output": "One observation row with workflow_count and provenance_step_count",
  "assertions": [
    {
      "field": "provenance_step_count",
      "op": "gte",
      "value": 1,
      "controls": [
        "si-7.1"
      ],
      "description": "Some workflow step generates provenance or attestations (slsa-github-generator, attest-build-provenance, cosign, --provenance)."
    }
  ],
  "cadence": "weekly",
  "automatable": "full",
  "caveats": "Detects the configuration, not the attestations themselves — verifying published attestations against a registry is appliance-phase work. It also detects provenance being GENERATED rather than VERIFIED before consumption, which is a distinct workflow pattern nothing here matches — the same distinction the batch-1 audit drew on SR-10.",
  "notes": "Empty-set discipline — Counter: repo-facts always emits exactly one summary row of workflow and provenance-step counts, so a repo with no CI at all violates honestly instead of passing over an empty set; an empty observation set here could only mean the collector never ran.",
  "empty_means": "unevidenced",
  "plain": {
    "checks": "Checks whether any CI workflow records where a build came from — a signed attestation or provenance document tying the artifact it published back to the commit that produced it.",
    "violation": "Nothing links a shipped artifact to the source it was built from. Anyone reviewing an incident later has to take the build's word for it, and an artifact built somewhere else looks exactly like the real one.",
    "fix": "Add a provenance or attestation step to the workflow that publishes your artifacts — GitHub's attest-build-provenance, the SLSA generator, or cosign — so each build signs a statement naming its own source commit."
  },
  "anchor": "commit"
}
