{
  "id": "sbom-exists-and-fresh",
  "ksi_ids": ["KSI-PIY-GIV"],
  "control_ids": ["cm-2.2", "cm-8"],
  "evidence": "A CycloneDX SBOM generated from the repository at the scanned commit, proving the component inventory exists and is current",
  "collection": { "kind": "pipeline", "collector": "syft" },
  "expected_output": "sbom.cdx.json (CycloneDX JSON) plus one observation row with format, spec_version, component_count, generated_at",
  "assertions": [
    {
      "field": "component_count",
      "op": "gte",
      "value": 1,
      "controls": ["cm-8"],
      "description": "The SBOM enumerates at least one component — an empty inventory is no inventory."
    },
    {
      "field": "generated_at",
      "op": "max_age_days",
      "value": 1,
      "controls": ["cm-2.2"],
      "description": "The SBOM was generated by this scan run, not copied in stale."
    }
  ],
  "cadence": "weekly",
  "automatable": "full",
  "notes": "syft over the working tree at the pinned commit; the SBOM artifact is the input to osv-scanner downstream. Empty-set discipline — Skip over a counter: no syft and the collector skips with a stated reason; when it does run it emits exactly one summary row carrying the component count and the generation instant, so an empty observation set could only mean the collector never ran — and both assertions are row-wise, which over zero rows would pass vacuously.",
  "empty_means": "unevidenced",
  "plain": {
    "checks": "Builds an inventory of every component the repository is made of — a software bill of materials — and checks that this scan generated it rather than finding an older one lying around.",
    "violation": "Either nothing enumerates what your software is made of, or the list on hand describes an older version of it. Every question that depends on it — above all “are we affected by this advisory?” — gets answered against the wrong inventory.",
    "fix": "There is nothing to author here: the inventory is generated by the scan itself. An empty or stale one means the generator could not read the project, so check that manifests are committed and that dependencies are installed where it can see them."
  },
  "anchor": "commit"
}
